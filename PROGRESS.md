# Oathfall — 진행 상황

다크 판타지 턴제 카드게임 프로토타입. 이 문서는 "구현된 것 / 앞으로 계획 / 이슈"를
의미 있는 작업마다 갱신하는 1차 참고 자료다. 상세 설계/근거는 커밋 로그와 코드 주석에,
큰 방향 결정은 여기에 요약한다.

## 완료된 마일스톤

- **M1** — 로컬 프로토타입. 왕국 vs 왕국 미러전, 핫싯 + 더미 AI, 11노드 단일 전선.
- **M2** — 4종족 구현 (왕국/광신도/차원 괴물/감염체).
- **M3** — 대규모 개편:
  - 이동력(moveRange/movePoints) 도입 — 한 턴에 전 맵을 먹는 문제 해결.
  - 공용 "영향력" 자원 폐기 → 종족별 고유자원 2축 체계.
  - AP 자동 증가(라운드 기준, 공용 상한 4) + 종족별 추가 상한(차원 괴물/감염체는 자기
    티어 스탯 기준, 왕국/광신도는 수도 티어 기준).
  - 맵을 11노드 단일 전선 → 17노드(공용) + 종족별 수도 인컴 슬롯(1~3개, 종족 선택에
    따라 맵이 매 게임 동적으로 생성됨)으로 확장. 2개의 실제 전선(북/남) + 중앙 매립지.
  - 수도 티어 시스템(1~4) — 상위 카드는 티어를 달성해야 덱에 섞여 들어간다(죽은 패 방지).
  - 감염체 리디자인 — 감염은 중립 노드만 탈취, 이미 소유된 노드(수도 포함)는 탈취 대신
    디버프(생산 -50%/방어 -15%, 5라운드). 완료 지점은 영구 "포자 지대"가 되어 감염체
    부대를 강화(크립 유사) + 낮은 확률 자연 확산.
  - 외교(조약) 시스템 완전 배제. 종족 정체성은 "종족 특수 능력"(카드 없이 항상 적용되는
    패시브)으로 분리: 왕국-영지 총동원령, 광신도-출혈의 계약, 차원 괴물-차원 공명,
    감염체-자가 증식.
  - 종족별 건물 효율 차별화 — 왕국(티어 보너스)/광신도(고위험 고수익)/차원 괴물(차원문
    레벨에 비례해 스스로 강해짐)/감염체(포자 지대 위 보너스).
  - 20라운드로 확장, 손패 UI를 하단 전체 폭 트레이로 이동.
  - Cloudflare Pages 배포 파이프라인 구축(`.github/workflows/deploy-cloudflare.yml`).

## 구현된 것

- 순수 룰 엔진(`js/rules.js`): `initGame(seed, opts)` / `reduce(state, action)`, 결정론적
  시드 RNG(`js/rng.js`), 4종족 카드 정의(`js/cards.js`), 종족별 동적 맵 생성(`js/mapData.js`).
- 더미 AI(`js/ai.js`) — 메타데이터 기반 범용 로직, 이동력 체인 계획.
- 순수 DOM 렌더링(`js/render.js`) + 상태 비저장 진입점(`js/main.js`).
- 정적 사이트 배포: Cloudflare Pages (`https://oathfall-7e4.pages.dev/`).

## 앞으로 계획 — 온라인 멀티플레이

상세 설계는 계획 파일(에이전트 검토 완료, 승인됨) 참고. 방향: 비동기(턴제),
Cloudflare Workers + Durable Objects, 로컬 핫싯+AI 모드 유지, 방 코드/링크 공유 입장.

- [x] **Phase A** — 서버(`server/`: Worker + `GameRoom` Durable Object, 룸 생성/참가/상태조회/
  액션제출 엔드포인트, 시청자별 state 마스킹). `wrangler dev` + curl 스크립트로 검증 완료
  (룸 생성/참가, 양방향 손패 마스킹, seed 미노출, 턴 교대, 잘못된 턴/토큰/시트사칭 거부,
  action.player 위조 무시, 없는 방/꽉찬 방, CORS preflight — 전부 통과).
- [x] **Phase B** — 클라이언트 연동(`js/netClient.js`, `js/config.js`, `main.js`/`render.js`
  변경 — 모드 분기, viewerSeat 도입, 폴링, 방 만들기/참가/대기 화면). `wrangler dev` +
  정적 서버 + Playwright로 두 브라우저 컨텍스트를 띄워 end-to-end 검증 완료 (방 생성 →
  링크로 참가 → 턴 교대 폴링 → 카드 플레이 → ISSUE_MARCH까지 전부 통과, 상대 턴에도 내
  손패는 계속 보이는 것 확인).
- [ ] **Phase C** — 마무리(턴 상태 표시는 이미 됨; 남은 것: 방 없음/만료 에러 케이스 실제
  배포 환경에서 재확인, DO 정리 알람 30일 동작 확인, `.assetsignore`로 `server/` 정적
  업로드 제외, 실제 두 사람 간 실배포 테스트).
- [x] **배포**: Worker가 `https://oathfall-mp.nonggle4918.workers.dev`로 실제 배포됨.
  `CLOUDFLARE_API_TOKEN`은 Workers 배포 권한도 이미 있었음(추가 조치 불필요). 최초 배포는
  Durable Objects 마이그레이션 오류(무료 플랜은 `new_sqlite_classes` 필요, 코드 10097)로
  한 번 실패했다가 수정 후 성공. `js/config.js`의 `WORKER_BASE_URL`을 실제 주소로 갱신,
  CORS preflight도 실제 Pages 오리진(`https://oathfall-7e4.pages.dev`)으로 라이브 확인 완료.

## 이슈 · 알려진 트레이드오프

- AI는 단순 휴리스틱(더미)이다 — 정교한 전략적 판단은 하지 않는다.
- AI의 이동력 체인 계획은 전투가 끼는 순간 그 이후 이동을 계획하지 않고 멈춘다(결과가
  불확실하기 때문에 의도적으로 보수적으로 설계함).
- 수도 인컴 슬롯 좌표는 정적 배치 방식이라, 향후 노드 수가 더 늘어나면 겹침 방지를 위해
  좌표 계산 로직을 다시 봐야 할 수 있다.
- 온라인 멀티플레이 도입 시 기존 핫싯 UI의 `active.isAI` 게이팅을 `viewerSeat` 기반으로
  바꿔야 하는 지점이 여러 곳(`render.js`/`main.js`)에 있음 — 계획 파일 §0/§2.5 참고.
- Cloudflare Worker 배포용 `CLOUDFLARE_API_TOKEN`이 Pages 편집 권한만 있다면 Workers
  배포 시 403이 날 수 있음 — 필요 시 토큰 권한 추가는 사용자가 Cloudflare 대시보드에서
  직접 해야 함(샌드박스에서 확인/수정 불가).
